package util;

import model.Absence;

import java.util.List;

public class ExportPDF {
    public static void exportAbsencesToPdf(List<Absence> absences, String destinationPath) {
        // Implémenter l’export PDF avec la bibliothèque de votre choix
        System.out.println("Export des absences vers PDF : " + destinationPath);
    }
}
